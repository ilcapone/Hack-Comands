smbclient.py this program list network shares and can access, put and get files, read/write network file share.
smbclient.py {DOMAIN}/{USER}@{HOST}

smbclient this program list network shares and can access,put and get files, read/write network file share.
List shares with login
smbclient -L \\{HOST} -U {DOMAIN}\\{USERNAME} -P {PASSWORD}
Access shares without login
smbclient \\\\{HOST}\\SHARE -U {DOMAIN}\\{USERANME} -P {PASSWORD}
